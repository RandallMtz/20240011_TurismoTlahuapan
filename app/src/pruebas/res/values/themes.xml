<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppMovTlahuapan" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blueMain</item>
        <item name="colorPrimaryVariant">@color/green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/black</item>
    </style>

    <!-- Base DatePickers theme. -->
    <style name="Theme.AppMovTlahuapan" parent="Theme.AppCompat.DayNight.Dialog">
        <item name="colorAccent">@color/blueMain</item> <!-- Fondo blanco -->
        <item name="android:textColorPrimary">#000000</item> <!-- Color del texto de las opciones -->
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/blueMain</item>
        <item name="colorPrimaryVariant">@color/blueMain</item>
        <item name="colorOnPrimary">@color/blueMain</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/black</item>
    </style>
</resources>